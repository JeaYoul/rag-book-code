# 게이트웨이. 프록시(서버) 기능까지 쓰려면 [proxy] 가 필요하다.
#
# 버전을 못 박아라.
#   널리 쓰이는 도구의 특정 버전에 자격 증명을 훔치는 코드가 섞여 배포된 사고가 있었다.
#   내 서버에서 검증한 버전을  pip freeze | grep litellm  으로 확인해 아래처럼 고정한다.
#   예)  litellm[proxy]==1.xx.y
litellm[proxy]
