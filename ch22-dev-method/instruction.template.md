# 지시문 — <한 줄 제목>

<!--
  22장 3단계. 대화에서 나온 결론을 AI 가 실행할 수 있는 문장으로 옮기는 문서.
  이 단계를 건너뛰면 늘 실패했다. 대화의 결론은 대화창 안에만 있고, 창을 닫으면 사라진다.

  쓰는 법: 이 파일을 복사해 채우고, AI 코딩 도구에게 그대로 준다.
  분량은 한 화면을 넘지 않는 편이 낫다. 넘으면 일을 두 개로 쪼개라.
-->

작성일: 2026-__-__   |   관련 대화: <어디서 정했는지 한 줄>

## 1. 무엇이 문제인가

지금 무엇이 잘못 돌고 있는지, 또는 무엇이 없는지. **증상을 적어라. 원인을 적지 마라.**
원인을 미리 못 박으면 AI 가 그 가정 안에서만 고친다.

- 증상:
- 언제 그런가:
- 재현하는 방법:

## 2. 무엇을 만들 것인가

바뀐 뒤의 모습을 적는다. 완료 조건을 검사 가능한 문장으로.

- [ ] …하면 …이 나온다
- [ ] …일 때 …로 처리된다

## 3. 어디를 고치는가

| 파일 | 무엇을 |
|---|---|
| `src/____.py` | |
| `sql/____.sql` | |

## 4. 건드리지 말 것

**이 절이 지시문의 절반이다.** 여기 적지 않은 것은 건드려도 된다는 뜻이 된다.

- 이 작업에서 데이터베이스 스키마는 바꾸지 않는다.
- `____` 함수의 호출 규약은 그대로 둔다. 다른 곳에서 쓴다.
- 새 라이브러리를 추가하지 않는다. 필요하면 먼저 말한다.

## 5. 어떻게 확인하는가

돌려 볼 명령을 미리 적어 둔다. 이것이 있으면 "됐다고 하는데 안 되는" 상황이 줄어든다.

```bash
# 고치기 전
# 고친 뒤
# 기대하는 출력
```

## 6. 하지 않아도 되는 것

범위를 닫는 자리. 여기 적으면 AI 가 친절하게 더 해 주다가 일을 키우는 것을 막는다.

- 성능 최적화는 이번에 하지 않는다.
- 화면 정리는 다음 작업으로 미룬다.
